type: object
title: MessageResponse
description: "Response containing the status of the message"
example:
  message: "Message send with success"
properties:
  message:
    type: string
    description: "A validation message"
    example: "Message send with success"
required:
  - message