server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;
    
    # Needed with Angular SPA to prevent 404 errors after page reload ; redirect all routes to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
